network:
  bind_host: 0.0.0.0

http:
  port: 9200

transport:
  tcp:
    port: 9300

path:
  data: /usr/share/elasticsearch/data
  logs: /usr/share/elasticsearch/logs

node:
  name: ${HOSTNAME}

script.disable_dynamic: false
